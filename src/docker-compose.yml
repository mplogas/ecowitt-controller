name: ecowitt-dev
services:
  mosquitto-dev:
    container_name: mosquitto-dev
    restart: unless-stopped
    image: docker.io/library/eclipse-mosquitto:latest
    ports:
      - 1883:1883
      - 8883:8883
      - 9001:9001  
    volumes:
      - type: bind
        source: ~/docker/config/mosquitto
        target: /mosquitto/
  homeassistant-dev:
    container_name: homeassistant-dev
    restart: unless-stopped
    image: "ghcr.io/home-assistant/home-assistant:stable"
    depends_on:
      - mosquitto-dev
    ports:
      - 8123:8123
    volumes:
      - type: bind
        source: ~/docker/config/homeassistant
        target: /config
      - type: bind
        source: /etc/localtime
        target: /etc/localtime
        read_only: true

#  ecowitt-controller-dev:
#    container_name: ecowitt-controller-dev
#    restart: unless-stopped
#    image: "mplogas/ecowitt-controller:dev-latest"
#    depends_on:
#      - mosquitto-dev
#    ports:
#      - 5656:8080
#    volumes:
#      - type: bind
#        source: ~/docker/config/ecowitt-controller
#        target: /config